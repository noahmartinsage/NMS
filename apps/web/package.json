{
  "name": "@repo/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "pnpm run build && pnpm run start"
  },
  "devDependencies": {
    "@types/node": "^20.14.11",
    "typescript": "^5.4.5"
  }
}
